# Sentry disconnected traces reproduction

## Setup

1. Configure `.env` file with `SENTRY_DSN` env
2. Run `docker compose up`
3. Go to `http://localhost:8080`
4. Observe disconnected traces in Sentry UI
